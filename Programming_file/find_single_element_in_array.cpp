#include<bits/stdc++.h>
using namespaces std;
int numfind(vector<int>& v){
    for(int i=0;i<v.size();i++){
        if(find(v[i])) 
    }
}
int main(){
     vector<int> v = {};
     cout<<numfind(v)<<endl;
    return 0;
}